// M11B.4 — Customer Market Intelligence entry point.
// Renders the 4-tab customer experience: Today | Theme Map | Signals | Universe.
// No operator tabs, no private bot data, no internal trading concepts.
import CustomerApp from "@/views/CustomerApp";

export default function CustomerPage() {
  return <CustomerApp />;
}
