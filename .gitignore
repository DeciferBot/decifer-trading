# Environment & secrets
.env
.env.local
.env.production
*.pem
*.key

# Data files (large or regenerable — small critical files are tracked)
data/*.csv
data/*.parquet
data/historical/
equity_history.json
favourites.json

# Large / regenerable data files that must never be committed
data/signals_log.jsonl
data/signals_log_historical.jsonl
data/signals_log_archive_*.jsonl
data/signals_typed.jsonl
data/audit_log.jsonl
data/sec_tickers.json
data/*.bak
data/raw/
data/features/

# Runtime state — generated by the bot, machine-specific, never commit
data/positions.json
data/orders.json
data/trades.json
data/capital_base.json
data/hwm_state.json
data/equity_history.json
data/decision_history.jsonl
data/last_decision.json
data/settings_override.json
data/pattern_library.json
data/ic_weights.json
data/ic_weights_history.jsonl
data/av_rate_limit.json
data/alpha_decay_cache.json
data/options_attempt_ledger.json
data/candle_gate_audit.json
data/feature_activation_log.json
data/advisor_log.json

# Stray log that sometimes lands in the project root
signals_log.jsonl

# One-off recovery / debug scripts
recover_equity_history.py

# Logs
logs/
weekly_review.txt

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/
dist/
build/

# Jupyter
.ipynb_checkpoints/

# Old git backup (leftover)
.git_old/

# Claude Code internal worktrees and memory
.claude/worktrees/
.claude/memory/

# One-off files
CLAUDE.md.bak
chief-decifer/state/

# Generated by build_brain.py (regenerable, not hand-written)
decifer-brain.html
brain_cache.json

# OS
.DS_Store
Thumbs.db
.~lock.*

# macOS app bundles (machine-specific binaries)
*.app/

# Generated docs (can be regenerated)
chief-decifer-dashboard-preview.html
chief_icon.png

# Office temp files
~$*
