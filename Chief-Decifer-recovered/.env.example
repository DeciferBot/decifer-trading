# Path to the decifer-trading repo on this machine (needed for git history, tests, code health)
DECIFER_REPO_PATH=/path/to/decifer-trading

# How often the dashboard auto-refreshes (milliseconds)
REFRESH_INTERVAL_MS=30000

# Dashboard port
PORT=8181
