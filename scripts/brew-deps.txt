# Decifer Trading — Homebrew system dependencies
# Add a line here whenever a new brew package is required.
# scripts/setup.sh reads this file automatically on rebuild.
python@3.11
ta-lib
node
git
uv
